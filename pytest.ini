[pytest]
# 'auto' mode tells pytest-asyncio to automatically handle 
# any 'async' test or fixture it finds without needing markers everywhere.
asyncio_mode = auto

# This suppresses the warnings about loop scopes which 
# is standard in newer versions of pytest-asyncio.
asyncio_default_fixture_loop_scope = function